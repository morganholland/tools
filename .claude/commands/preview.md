Open the specified HTML tool in the default browser for testing.

Tool to preview: $ARGUMENTS

If no argument provided, list available HTML files and ask which one to open.

Use the `open` command on macOS to launch the file in the default browser.
